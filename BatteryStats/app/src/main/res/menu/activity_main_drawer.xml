<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_notification"
            android:icon="@drawable/ic_notification"
            android:title="@string/notification" />
        <item
            android:id="@+id/nav_battery_history"
            android:icon="@drawable/ic_battery_history"
            android:title="@string/battery_history" />
        <item
            android:id="@+id/nav_estimated"
            android:icon="@drawable/ic_estimated"
            android:title="@string/estimated_power_use" />
        <item
            android:id="@+id/nav_detail"
            android:icon="@drawable/ic_detail_stats"
            android:title="@string/detailed_stats" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Report" />
        </menu>
    </item>

</menu>
